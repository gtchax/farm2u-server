import { router as productRouter } from "@/routes/product.route";
import { router as refreshRouter } from "@/routes/refresh.route";
import { router as authRouter } from "@/routes/auth.route";
import { router as userRouter } from "@/routes/user.route";
import { router as rootRouter } from "@/routes/root.route";

export { productRouter, userRouter, authRouter, refreshRouter, rootRouter };
